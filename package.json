{
  "name": "shared-settings-manager",
  "displayName": "Shared Settings Manager",
  "description": "Automatically sync shared team settings from settings.shared.json into settings.json",
  "version": "0.1.36",
  "version_instructions": "increment only the patch number while doing dev iteration",
  "publisher": "artificial-necessity",
  "repository": {
    "type": "git",
    "url": "https://github.com/jeske/VSCodeExt_SharedSettingsManager"
  },
  "bugs": {
    "url": "https://github.com/jeske/VSCodeExt_SharedSettingsManager/issues"
  },
  "homepage": "https://github.com/jeske/VSCodeExt_SharedSettingsManager#readme",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": []
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "lint": "eslint src --ext ts",
    "bump-version": "node scripts/bump-version.js",
    "package": "npm run bump-version && vsce package --allow-missing-repository",
    "build": "npm run compile && npm run package"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^8.x",
    "@typescript-eslint/parser": "^8.x",
    "@vscode/vsce": "^3.x",
    "eslint": "^9.x",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1"
  }
}
